# 新塬镇应急避险指挥系统 · 更新日志

> 在线地址：https://bestfly438.github.io/xymap/
> 仓库：https://github.com/bestfly438/xymap （master 分支，GitHub Pages 自动部署）

---

## 2026-08-28 · v2.3 无障碍与界面打磨（ui-ux-pro-max / impeccable / improve-animations 四 skill 应用）

### 改动内容
1. **全站补无障碍支持**（`dashboard.html`、`map.html`、`index.html`、`health.html`、`msg.html`、`patrol.html`、`team.html`、`photo.html`）
   - 新增 `prefers-reduced-motion` 支持：系统开启"减弱动态效果"的用户，脉冲/闪烁/位移动画自动停用，避免眩晕。
   - 新增 `:focus-visible` 键盘焦点环：键盘导航（Tab）时能清楚看到当前焦点，符合政府/应急系统无障碍要求。
2. **去 AI 味（界面打磨）**
   - 列表项装饰性彩色左边框由 4px/3px 收细为 2px（调度指令、预警消息、巡查记录），红色/橙色/绿色语义色标保留——重要程度仍一眼可辨。

### 涉及文件
- 更新上述 8 个页面、`version.txt`、`CHANGELOG.md`

---
---

## 2026-08-28 · v2.2.1 修复应急队伍页空白

### 问题
"应急队伍"页（team.html）里各村防汛应急队伍显示为空。

### 原因
- 队伍数据文件 `village_teams.js` 属私有数据，只能登录后经腾讯云函数 `/api/data` 按**文件名**取数（与数据看板一致）。
- 该页却按 `{village:'全部'}` 请求并找 `teams` 字段，云函数返回里根本没有，导致 `VILLAGE_TEAMS` 一直是空。
- 页面还缺少静态村列表兜底，直接进页时 `VILLAGE_ORDER` 为空，即使有数据也渲染不出村列表。

### 修复（`team.html`）
- 改用 `loadSecureData(['village_teams.js'])` 登录取数，与看板同源；失败自动重试 2 次。
- 增加静态 10 村列表兜底（优先使用登录时带过来的 `xyc_village_order`）。

---
---

## 2026-08-28 · v2.2 消息中心与巡查记录独立页

### 新增功能
1. **消息中心独立页**（`msg.html`）：汇总镇级/管理员下发的调度指令与各村的回复情况
   - 管理员/镇级可查看全部村庄的调度消息，村级/社长仅看本村。
   - 支持按村、按状态（待回复/已回复）筛选；村级干部可直接点"回复"填写处置情况。
2. **巡查记录独立页**（`patrol.html`）：原来要进应急地图才能找到巡查记录，现在左侧菜单直达
   - 按村筛选 + 按巡查人筛选 + 统计卡片（记录总数 / 参与人数 / 最近巡查时间）。
   - 一键导出表格（CSV，Excel 可直接打开）：支持导出当前筛选记录，管理员/镇级可一键导出全镇记录。
   - 数据源为飞书云端（云端永久保存），本机 localStorage 为缓存备份，离线也能看。
3. **左侧菜单统一调整**（`dashboard.html`、`photo.html`、`team.html`、`msg.html`、`patrol.html`）
   - "数据台账"新增"巡查记录"入口。
   - 新增"消息与指令"分组：消息中心、应急队伍。

### 涉及文件
- 新增 `patrol.html`
- 更新 `dashboard.html`、`photo.html`、`team.html`、`msg.html`、`version.txt`、`CHANGELOG.md`

---
---

## 2026-08-04 · v2.1 安全体系升级（访问控制重构）

### 一、为什么升级
原系统把访问密钥和飞书凭证明文写在前端文件里，而网站必须托管在公开仓库上，**任何人查看源码即可拿到全部密钥、甚至直接操控数据表**。本次把安全校验全部收进服务端（腾讯云函数），从根上堵住泄露。

### 二、核心改动

1. **密钥改为服务端校验**（`index.html`、`feishu_config.js`、腾讯云函数）
   - 密钥列表从公开源码移除，改存飞书"密钥管理"表，登录时由腾讯云函数校验。
   - 旧 10 个密钥全部作废，重新生成 **37 个新密钥**：村级 30（10 村 × 3）+ 镇级 5 + 管理员 2。
   - 密钥分配表在本地文件 `密钥分配表.txt`，**严禁上传到 GitHub**。

2. **设备绑定（替代 IP 锁定）**（腾讯云函数）
   - 首次登录成功后，该手机浏览器生成专属设备标识并登记；**每密钥最多绑定 3 台设备**。
   - 换设备 / 清缓存后需重新输密钥；绑定满 3 台后再换设备会提示联系管理员解绑。
   - 优点：不受手机网络 IP 变化影响（IP 会频繁变），不会误伤正常使用。

3. **三级权限体系**（腾讯云函数强制校验 + 前端界面控制）
   | 能力 | 村级密钥 | 镇级密钥 | 管理员密钥 |
   |---|---|---|---|
   | 查看全镇数据 | ✅ | ✅ | ✅ |
   | 查看巡查记录 | ✅ | ✅ | ✅ |
   | 添加巡查记录 | 仅本村 | 所有村 | 所有村 |
   | 删除/修改巡查记录 | ❌ | ❌ | ✅ |
   | 修改村庄数据 | ❌ | ❌ | ✅ |
   | 密钥管理 / 解绑 / 登录记录 | ❌ | ❌ | ✅ |
   - 权限在服务端强制拦截（改前端代码也绕不过），前端仅做界面隐藏。

4. **飞书凭证移入服务端**（`feishu_config.js`、腾讯云函数）
   - 前端不再包含飞书 appSecret，所有飞书读写统一走腾讯云函数 `/api/feishu`（会话校验后转发）。即使看源码也无法再操控飞书数据表。

5. **管理员系统管理面板**（`dashboard.html`）
   - 顶栏新增"⚙ 系统管理"（仅管理员可见）：密钥列表、重置密钥（换新密钥）、解绑设备、启用/禁用密钥、查看登录记录（时间/IP/结果）。

### 三、登录记录
每次登录（成功/失败）写入飞书"登录记录"表，含密钥标签、角色、IP、设备、结果，管理员可在系统管理中查看。

### 四、涉及文件
- 前端：`index.html`、`map.html`、`dashboard.html`、`feishu_config.js`、`keys.js`、`CHANGELOG.md`
- 服务端：腾讯云函数 `tencent_scf_proxy.js`（需在腾讯云控制台手动更新部署）
- 飞书多维表格：新增「密钥管理」表 `tblpgtGwzNVa7ZpG`、「登录记录」表 `tbltnAR3WuzxyqP8`

### 五、使用提醒
- 管理员登录后进入"系统管理"，可随时重置/禁用某村密钥、解绑设备。
- 用户换手机登录时提示"已绑定3台设备"，需管理员在系统管理中"解绑"后重新登录。
- 腾讯云函数有默认访问频率限制，若短时间内大量登录请求可能提示"请求频繁"，属正常保护。

---

## 2026-08-06 · v2.1.3 移动端底部工具栏（手机看地图不再被按钮遮挡）

### 改动内容
1. **手机端底部工具栏**（`map.html`）：原来分散在四角的按钮（信息面板📋、图例、右下角6个图层圆钮）全部收进屏幕底部一条工具栏：
   - 📋 信息：打开村庄信息面板
   - ⚠ 转移：直接打开当前村的"转移谁"页签
   - 📝 巡查：直接打开当前村的"巡查记录"页签（未选村庄时会提示先选村）
   - 🗺 图层：弹出图层面板，开关 隐患点/受威胁区/撤离路线/安置点/道路，面板内可直达"查看图例"
   - 📍 定位
2. **图层面板**：点击地图空白处自动收起（与图例收起逻辑一致）。
3. PC 端不受影响（工具栏仅手机模式显示，原按钮布局保持不变）。

### 涉及文件
- `map.html`、`version.txt`（版本号 v20260806-07）

---

## 2026-08-06 · v2.1.2 切坡建房户数口径修正（与地图点位一致）

### 问题
此前系统里"切坡建房X户"统计来自《新塬镇一页纸数据》Excel 中每个隐患点的"威胁X户Y人"相加（合计约181户），与地图上实际标注的切坡建房隐患户点位（SHP，205户）对不上，各村数字也全部对不上。

### 改动
1. **切坡建房户数改为按 SHP 隐患点位统计**（`dashboard.html`、`map.html` 的 `getVillagePlanStats`）：
   - 每户一个点，按点位所属村统计，与地图红点一一对应。
   - 全镇 205 户：上塬36、东塬17、孟塬19、常家坪37、新塬8、杨坪5、杨家河坝11、甘岔52、老庄20，刘家湾社区0。
   - 村名做 SHP 简称→全名映射（常坪村→常家坪村、河坝村→杨家河坝村）。
2. **"隐患点数量 / 受威胁人口"仍沿用一页纸数据**（该口径表示需撤离/受威胁的户与人口，与切坡建房户数是两个概念）。

### 涉及文件
- `dashboard.html`、`map.html`、`version.txt`（版本号 v20260806-06）

---

## 2026-08-06 · v2.1.1 图例优化（手机端不再遮挡地图）

### 改动内容
1. **图例瘦身**：移除"国道（G）"图例项——新塬镇范围内实际无国道经过，原图例显示了地图上看不到的内容，现已去掉。
2. **手机端图例改为可收起/展开**（参考高德、百度等主流地图APP做法）：
   - 手机打开地图时，图例默认折叠为一个左下角的小"图例"按钮，不再占据约 1/4 屏幕。
   - 点击按钮展开查看全部图例；再点按钮或点右上角 ✕、或点击地图空白处，都会自动收起。
   - 展开时图例面板限高（不超过屏幕 56%），内容多时可上下滑动。
   - PC 端显示方式不变（图例常驻）。
3. 图例底部说明文字精简，删去具体路名示例。

### 涉及文件
- `map.html`、`version.txt`（版本号 v20260806-05）

---

## 2026-08-04 · v2.0 云端化与功能增强（本次重大更新）

### 一、新增功能

1. **巡查记录云端永久保存（飞书多维表格）**
   - 巡查记录不再只存本机浏览器，改为写入飞书多维表格（Base："新塬镇应急系统数据"），**退出登录、换设备后记录仍在**，随时可看、可导出 Excel。
   - 涉及文件：`feishu_config.js`（新增）、`map.html`
   - 数据表：巡查记录表 `tblz3JMnhmwDuNld`、村庄数据表 `tblwAxlJ9Ue5XNS1`

2. **管理员改数据 · 多账号同步**
   - 管理员在"编辑模式"下修改的村庄数据（责任人、电话等），自动同步到飞书云端；其他账号登录时自动拉取云端最新数据。多人协同不再"各改各的"。
   - 涉及文件：`dashboard.html`

3. **防汛应急指南页面**（`guide.html` 新增）
   - 主界面顶栏新增"🚨 防汛指南"入口。
   - 内容七板块：预警信号等级、应急响应流程、转移避险要点、山洪滑坡泥石流避险、应急准备清单、自救互救常识、紧急联系电话。

4. **导航功能（隐患点 → 导航前往）**
   - 地图上点击红色隐患点弹窗新增"🧭 导航前往"按钮：
     - **手机端**：唤起高德地图 App 直接导航。
     - **电脑端**：自动获取当前位置，调用高德 Web 服务 API 在地图上绘制驾车路线，显示距离与预计时间，可一键清除。
   - 高德 Web 服务 Key 已配置于 `keys.js` 的 `AMAP_KEY`（调用方式为 JSONP，无跨域问题）。
   - 涉及文件：`map.html`、`keys.js`

5. **主界面重新设计（DeepSeek 官网风格）**
   - 登录页、数据看板、地图顶栏统一改为**蓝紫渐变**主视觉（DeepSeek 品牌配色），更简洁现代。
   - 保留红/橙警示语义色（预警横幅、风险等级、隐患点等），不影响应急辨识。
   - 涉及文件：`index.html`、`dashboard.html`、`map.html`

### 二、问题修复

1. **浏览器直连飞书 API 被 CORS 跨域拦截**
   - 飞书开放接口不带 `Access-Control-Allow-Origin` 头，网页端直连必然失败。
   - **解决方案**：部署腾讯云函数（SCF，事件函数 + 函数URL）做转发代理，为响应补加 CORS 头。代理地址配置在 `feishu_config.js` 的 `apiBase`。
   - 腾讯云函数代码备份：项目根目录 `tencent_scf_proxy.js`。

2. **飞书 API 返回格式解析错误**
   - 飞书"查询记录"返回的是**列数组格式**（`data.data.data` + `data.data.fields` + `data.data.record_id_list`），原代码按对象数组解析，导致读不到数据。已新增 `feishuRowsToObjects()` 统一转换（`feishu_config.js`）。

3. **API 请求 URL 重复 `/open-apis` 前缀**（404）——已修正为单一前缀。

4. **巡查记录删除按钮 id 未加引号** —— 云端记录 id 为 `recXXX` 格式，未加引号导致点击删除报 JS 错误；同时云端保存后回写 record_id，删除可同步删云端。

5. **图例/图层控制遮挡侧边面板按钮** —— 图例(`#legend`)和图层控制(`#layer-ctrl`) z-index 降至 996，低于侧边面板(`#side-panel` 998)。

6. **旧 `#village-info` 面板与 `#side-panel` 相互干涉** —— 已统一为单一侧边面板，顶部加关闭按钮。

7. **定位偏移（我的位置不准）** —— 浏览器定位返回 WGS84，地图底图为高德 GCJ02，已在 `locateMe()` 和巡查定位中加 WGS84→GCJ02 转换。

### 三、部署与运维要点

- **部署方式**：推送到 GitHub master 分支，GitHub Pages 自动构建（Settings → Pages → Deploy from a branch: master / root）。
- **外部依赖**：
  | 服务 | 用途 | 国内访问 |
  |------|------|---------|
  | GitHub Pages | 网站托管 | 较慢，偶尔超时，需刷新重试 |
  | 高德地图瓦片/API | 底图、导航 | 快 ✅ |
  | 飞书开放平台 | 云端数据 | 快 ✅ |
  | 腾讯云函数 | CORS 代理 | 快 ✅ |
  | unpkg.com | Leaflet 库 | 较慢（如更慢可下载到本地） |
  | open-meteo | 天气数据 | 较慢 |
- **密钥**：访问密钥在 `keys.js`（`ACCESS_KEYS`），管理员密钥 `XYC-2024-ADMIN`；高德 Key 在 `AMAP_KEY`。

---

## 2026-08-04 · v1.3 巡查记录与界面优化

- 新增村级"巡查记录"子菜单：巡查人、自动 GPS 定位（WGS84→GCJ02）、自动天气、照片压缩上传（≤200KB）、描述、CSV 导出。
- 顶栏新增 PC/手机版切换按钮，偏好记忆在 localStorage。
- 修复巡查照片预览遮挡问题。

## 2026-08-02 · v1.2 村级预案地图

- 生成各村独立预案地图（隐患区、撤离路线、安置点、联系人）。
- 地图支持隐患点点击查看户主详情、撤离路线高亮、安置点定位。

## 2026-07-29 · v1.0 系统上线

- 搭建数据看板 + 应急避险地图 + 预警（含精简河道/道路数据）。
- 访问密钥机制（XYC-2024 系列）。
- 定位、图层切换、村庄下拉等基础功能。

---

## 维护经验与避坑记录

1. **免费 GitHub 账号的私有仓库不支持 GitHub Pages** —— 曾尝试把仓库设为私有，导致网站直接 404 下线；免费账号必须保持仓库公开才能托管 Pages。数据安全请靠**飞书表格权限**管控（不公开的敏感数据不要放进网站源码）。
2. **飞书 API 不能从浏览器直连**（无 CORS 头），必须经代理；腾讯云函数方案免费且国内稳定。
3. **飞书多维表格接口细节**：
   - 创建记录：POST body 直接传字段 Map（**不包 `fields`**），返回 `data.record_id_list`。
   - 查询过滤：`{"logic":"and","conditions":[["字段名","==","值"]]}`（数组三元组）。
   - 查询返回为列数组，用 `feishuRowsToObjects()` 转换。
   - 应用开通权限后必须**发布应用版本**才生效。
4. **浏览器/CDN 缓存**：更新后查看需强制刷新（Ctrl+Shift+R），否则会看到旧版文件。
5. **服务端/第三方配置**：飞书 Base、腾讯云函数、高德 Key 的配置修改后，务必在网页上实测（提交记录 → 换账号查看 → 删除），避免"看着配好了其实没通"。
